meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands: null
  artifacts:
    ./dist: /
run:
  command: npx serve -s dist
  persistenceMount: /data
  containerPort: 80