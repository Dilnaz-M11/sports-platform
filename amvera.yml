meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands:
    - npm cache clean --force
    - npm install
    - npm run build
  artifacts:
    dist: /
run:
  command: npx serve -s dist
  persistenceMount: /data
  containerPort: 80
